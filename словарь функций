max(1, 10, 3)  # 10 В примере выше функция max() находит максимальное значение среди переданных параметров.
# max(arg1, arg2, *args[, key])
min(1.10,3)
pow (a,b)  # a число b степень 
round(a,b)  #округление а число b коли-во знаков после зпт
abs()  # возврашает модуль числа
random()# генерирует от 0 до 1 число
# Возвращает индекс первого вхождения буквы в строку
name.find('t')  # 2
# Переводит в нижний регистр
name.lower()   # Переводит в нижний регистр
# Заменяет одну подстроку другой
name.replace('on', 'off')  # 'Pythoff'
 bit_length() # вычисляет количество бит, необходимых для представления числа в двоичном виде
str.capitalize() # вернет копию строки str с первым символом в верхнем регистре, а остальные символы будут в нижнем регистре.
.strip() # пробельные символы в конце или начале строки. Обычно их вырезают с помощью метода 

